# VLink Backend

This folder hosts the Next.js API service for VLink under `app/api/*`.

## Run locally

1. Copy `.env.local.example` to `.env.local` and fill all required keys.
2. Install and run:

```bash
npm ci
npm run dev -- --port 3001
```

Health page: `/`
API base: `/api/*`
